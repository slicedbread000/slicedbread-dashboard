name: Daily Refresh

on:
  schedule:
    - cron: "5 9 * * *" # 09:05 UTC daily
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping production to refresh cache
        run: curl -sS https://slicedbread-dashboard.vercel.app/api/health > /dev/null
